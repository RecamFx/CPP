// Estructura basica
// Cada vez que escribamos algo debemos cerrarlo con un punto y coma;

// Se compone por la libreria iostream, que nos facilita el uso de la consola

// Usamos #include para incluir una libreria
#include <iostream>
// Iostream desbloquea dos comandos nuevos:
// cin >> valor; Es un ingresador de valores via consola
// cout << valor; Imprime en la consola


// Escribimos esto debido a que si no en vez de escribir cout deberiamos poner std::cout o cin cada vez
// Es para acortarnos el codigo
using namespace std;


// Dentro de main vamos a escribir todo el codigo
int main(){

}