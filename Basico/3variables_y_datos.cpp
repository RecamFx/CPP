#include <iostream>
using namespace std;

int main(){

    // Definiendo variables de tipo:
    
    int numeroEntero = 10; // Los int son numeros enteros, es decir, sin coma
    float numeroFlotante = 10.0; // Los flotantes son numeros con coma
    bool trueFalse = false; // Los booleanos son valores True o False, 1 o 0
    char letra = 'a'; // Los char son letras
    string cadenaDeCaracteres = "Cadena"; // Y los strings son cadenas de caracteres

    // Diferencias:
    // Los int tienen menos capacidad de almacenamiento que un float
    // Los chars se escriben con comillas simples, mientras que los strings se escriben con comillas dobles

    // Como nombrar variables:
    // Solo letras y/o numeros
    // No comienzan con numeros
    // Se usa: camelCase, tecnica para nombrar a una variable, se basa en escribir en mayuscula la primera letra de una palabra
    // Y tamien: snake_case, tecnica para nombrar a una variable, se basa en escribir _ guines bajos entre las palabras

    // Definir valores:
    // Se pueden definir valoreres o dejarlos en blanco. Se recomienda definir valores para que no pasen errores comunes
    // Variables sin valores definidos:
    int num;
    float flotant;
    bool siono;
    char letter;
    string cadenas;

}